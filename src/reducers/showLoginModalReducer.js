import { SHOW_LOGIN_MODAL } from "../constants/actionTypes"

const initialState = {
	showLoginModal: false
}

function showLoginModal(state = initialState, action){
	return state
}

export default showLoginModal